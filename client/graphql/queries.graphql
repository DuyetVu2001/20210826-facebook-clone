query ListUsers {
	listUsers {
		id
		username
		avatar
	}
}

query GetCurrentUser {
	getCurrentUser {
		id
		username
		avatar
	}
}

query ListPosts {
	listPosts {
		id
		title
		content
		keyword
		image
		userId
		user {
			id
			username
			avatar
		}
		createdAt
		updatedAt
	}
}
